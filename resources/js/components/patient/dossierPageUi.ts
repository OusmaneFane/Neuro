/** Conteneur vertical standard des sous-pages dossier (aligné sur Examens complémentaires). */
export const DOSSIER_PAGE_STACK = 'space-y-8 pb-10';

/** Carte de formulaire neutre : dégradé léger, ring, ombre. */
export const DOSSIER_NEUTRAL_CARD =
  'overflow-hidden border-0 bg-linear-to-br from-slate-50/80 via-white to-white p-0 shadow-lg ring-1 ring-slate-200/80';

/** Même style, sans masquer les listes flottantes (SearchableSelect dans la colonne). */
export const DOSSIER_NEUTRAL_CARD_OVERFLOW_VISIBLE =
  'overflow-visible border-0 bg-linear-to-br from-slate-50/80 via-white to-white p-0 shadow-lg ring-1 ring-slate-200/80';

/** Bandeau d’en-tête de section dans une carte dossier. */
export const DOSSIER_CARD_HEADER =
  'border-b border-slate-100 bg-linear-to-r from-slate-100/50 to-transparent px-6 py-4';

/** Corps sous l’en-tête de carte. */
export const DOSSIER_CARD_BODY = 'p-6';
